# Basic dependencies
transformers
huggingface_hub
gradio

# llama-cpp-python, installed 'make install' in the root folder

# Only needed for GPTQ models
# auto-gptq and must be installed manually see README.md
urllib3==1.26.16

# Only needed for Huggingface Autrotrain
autotrain-advanced
